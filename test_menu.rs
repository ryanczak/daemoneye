use gtk4 as gtk;
use gtk::prelude::*;
use gtk::gio;

fn main() {
    let _menu = gio::Menu::new();
    let _group = gio::SimpleActionGroup::new();
    let _action = gio::SimpleAction::new("test", None);
}

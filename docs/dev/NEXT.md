# NEXT

**Active milestone:** M1 — Agent Tooling Improvements
(`docs/dev/milestones/M1-agent-tooling/README.md`)

**Active phase:** none. phase-03-script-exec-hardening is `done` (approved
first try, 2026-06-21). Phases 04–08 remain `todo` and undrafted.

Run `/rexymcp:architect next` to draft and select the next phase (phase-04,
remote-script-transfer — the ghost `ssh_target` script push).

Note: the original row-03 bundle was split — remote script transfer is now
phase-04, write-tool `target_pane` parity is phase-05, and namespace ACL /
execution-robustness / prompt-fixes are 06/07/08.

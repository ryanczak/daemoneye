# NEXT

**Active milestone:** M1 — Agent Tooling Improvements
(`docs/dev/milestones/M1-agent-tooling/README.md`)

**Active phase:** none. phase-07a-pane-targeting-and-cleanup-safety is `done` and
approved (approved_first_try). The original phase-07
("execution-robustness-and-tmux") was split into **07a** (done — the four `medium`
mechanical/safety fixes) and **07b** (the two `high` completion/exit-code items +
tmux leverage, drafted on demand).

Next: draft **07b** via `/rexymcp:architect next`. Phases 07b, 08, 09 remain `todo`.

# NEXT

**Active milestone:** M1 — Agent Tooling Improvements
(`docs/dev/milestones/M1-agent-tooling/README.md`)

**Active phase:** phase-07b-completion-and-exit-code-correctness (`todo`, drafted
2026-06-22, awaiting dispatch). It covers the two `high` items from the original
phase-07: local completion detection via the `DE_EXIT` latch, and non-zero
exit-code surfacing to the model.

The original phase-07 ("execution-robustness-and-tmux") was split into **07a**
(done — four `medium` mechanical/safety fixes), **07b** (this phase — the two
`high` completion/exit-code items), and **07c** (deferred — open-ended tmux-verb
leverage; drafted on demand only if a concrete site warrants it).

Next: dispatch **07b** via `/rexymcp:dispatch phase-07b-completion-and-exit-code-correctness`.
Phases 07b, 07c, 08, 09 remain `todo`.
